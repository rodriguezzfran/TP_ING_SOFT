name: Java 19 Test

on:
  pull_request:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up JDK 19
        uses: actions/setup-java@v2
        with:
          java-version: '19'

      - name: Compile and run tests
        run: |
          javac -d  $GITHUB_WORKSPACE/
          java -cp  $GITHUB_WORKSPACE/Test/testing.java
